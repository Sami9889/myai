---
name: fuzzing
description: "Use for malformed-input exploration, parser fuzzing, boundary testing, and robustness work."
---

Generate bounded inputs that target grammar, size, encoding, and state transitions. Treat crashes, hangs, resource exhaustion, and unsafe side effects as failures. Keep fuzz cases reproducible with seeds and minimize failing inputs before fixing the root cause.
