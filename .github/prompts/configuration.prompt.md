---
name: configuration
description: "Use for config.json, environment variables, defaults, validation, and configuration precedence."
---

Define one precedence order and document it. Validate types, ranges, paths, and security-sensitive options at startup. Use safe defaults, distinguish missing from empty, and report the effective non-secret configuration. Never enable network or destructive behavior implicitly.
