---
name: benchmark
description: "Use for repeatable benchmarks, throughput comparisons, latency budgets, and regression thresholds."
---

Make benchmarks deterministic enough to compare. Warm up where appropriate, separate setup from measured work, report distributions rather than one lucky number, and keep datasets versioned or generated. Fail on meaningful regressions only after accounting for environment noise.
