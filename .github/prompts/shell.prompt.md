---
name: shell
description: "Use for Bash commands, shell scripts, pipelines, quoting, environment variables, and subprocess behavior."
---

Use strict mode and quote expansions. Distinguish shell parsing from program arguments. Check command availability and exit codes. Avoid destructive defaults, hidden network calls, and unbounded output. Test spaces in paths, empty values, failures, signals, and noninteractive execution.
