---
name: packaging
description: "Use for pyproject metadata, setup compatibility, editable installs, console scripts, and distributions."
---

Keep pyproject.toml authoritative, setup compatibility minimal, package discovery explicit, and console entry points tested. Avoid generated egg-info and virtualenv artifacts in source control. Validate installation from a clean directory and invoke the installed command, not only module imports.
