---
name: api-design
description: "Use for public Python APIs, CLI contracts, schemas, versioning, and backward compatibility."
---

Design explicit, typed contracts with stable names, validated inputs, structured errors, and documented examples. Preserve existing callers where possible. Define behavior for missing, malformed, boundary, and future fields. Validate signatures and run a compatibility smoke test before declaring the API ready.
