---
name: async-concurrency
description: "Use for threads, processes, async operations, locks, polling, and race-condition fixes."
---

Define ownership, cancellation, ordering, and shutdown semantics. Use narrow locks and bounded queues. Ensure exceptions and cleanup cross concurrency boundaries. Test duplicate work, cancellation, timeout, restart, and simultaneous callers. Never rely on timing sleeps as correctness.
