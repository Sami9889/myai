---
name: data-pipeline
description: "Use for ingestion, indexing, preprocessing, token streams, and local retrieval pipelines."
---

Make transformations deterministic, bounded, observable, and restartable. Preserve source provenance and handle malformed records without corrupting later data. Measure memory and throughput on representative inputs. Keep local indexing offline unless network access is explicitly requested and gated.
