---
name: logging
description: "Use for structured logs, logger configuration, error records, and diagnostic output."
---

Log enough context to reproduce behavior without logging secrets, full user content, or hidden reasoning. Use stable levels and actionable messages. Keep library imports quiet by default and make file/stream destinations explicit. Validate failure paths and log rotation assumptions.
