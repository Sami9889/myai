#!/usr/bin/env bash
set -Eeuo pipefail
root="$(cd "$(dirname "$0")/.." && pwd)"
if [[ -x "$root/.venv/bin/python" ]]; then exec "$root/.venv/bin/python" -m cli_interface.repl "$@"; fi
exec python3 -m cli_interface.repl "$@"
